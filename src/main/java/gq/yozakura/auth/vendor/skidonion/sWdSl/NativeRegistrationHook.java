package gq.yozakura.auth.vendor.skidonion.sWdSl;

public class NativeRegistrationHook {
    public static void registerNativeMethods(int id, Class<?> clazz) {
        // The native loader used this hook to register JNI methods. In the restored
        // Java version every method is implemented directly, so this is intentionally empty.
    }
}

